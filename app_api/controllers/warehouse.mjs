const Warehouse = {
    async getAirplanesData(req, res) {
        // Return verfied data
        // (Attributes and data, which is allowed to be returned from chosen)
    }
}

export default Warehouse;